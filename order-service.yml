management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

spring:
  kafka:
    bootstrap-servers: marche-conclu-kafka.web.1:9092

notification:
  topic: notifications
